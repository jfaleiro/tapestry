dependencies {
	compile project(':tapestry-core')
	
	// test dependencies, pain in gradle
	testCompile project(path: ':tapestry-core', configuration: 'tests')
	
	compile 'com.hazelcast:hazelcast:3.5.3'
}
